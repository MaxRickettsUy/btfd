{
    "name": "btfd",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "start": "cd backend && npm install && npm start",
        "heroku-postbuild": "cd ui && (npm install && npm run build)"
    }
}